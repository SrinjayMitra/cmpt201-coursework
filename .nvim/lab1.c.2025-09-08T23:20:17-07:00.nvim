#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) {
  char buffer[32];
  char *b = buffer;
  size_t bufsize = 32;
  size_t characters;
  char* token;
  printf("entr your number\n");
  characters = getline(&b, &bufsize, stdin);
  printf("%zu", characters);
  printf("%s", buffer);
  while((token = strtok_r(b," ", &buffer)))
      printf("%s\n",token);
  return 0;
}
